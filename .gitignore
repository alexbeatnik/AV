# Portable ClamAV (219 MB of binaries + database) — downloaded separately, see README
clamav/

# YARA engine + rules — downloaded automatically (yara64.exe, YARA Forge rules)
yara/

# Build artifacts
AV.exe
AV.update.exe
AVUI.Tests.exe
*.pdb

# Local user settings
settings.ini
scans.log
# VirusTotal API key — a secret, lives in its own file so installs can't wipe it
vt.key

# Quarantine (infected files)
quarantine/
